{% extends 'base.html.twig' %}

{% block body %}
    {#<nav class="navbar navbar-toggleable-md navbar-light" style="background-color: #e3f2fd;">#}
    {#<button class="navbar-toggler navbar-toggler-right" type="button" data-toggle="collapse" data-target="#navbarNavAltMarkup" aria-controls="navbarNavAltMarkup" aria-expanded="false" aria-label="Toggle navigation">#}
    {#<span class="navbar-toggler-icon"></span>#}
    {#</button>#}
    {#<a class="navbar-brand" href="/">SportSHOP</a>#}
    {#<div class="collapse navbar-collapse" id="navbarNavAltMarkup">#}
    {#<div class="navbar-nav">#}
    {#<a class="nav-item nav-link active" href="/">Главная <span class="sr-only">(current)</span></a>#}
    {#{% for item in categories %}#}
    {#<a class="nav-item nav-link" href="{{ path('view-category', {'id': item.id}) }}" title="{{ item.description }}">{{ item.name }}</a>#}
    {#{% endfor %}#}
    {#</div>#}
    {#</div>#}
    {#</nav>#}
    <div id="wrapper">
        <div id="container">
            <div id="welcome">
                <h1><span>Добро пожаловать в</span> onlineStore
                </h1>
            </div>
            <div id="next">
                <h2>Каталог товаров: </h2>
                {% for item in categories %}
                    <p><a href="{{ path('view-category', {'id': item.id}) }}"
                          title="{{ item.description }}">{{ item.name }}</a></p>
                {% endfor %}

                <h2>Меню: </h2>
                <a href="{{ path('contacts') }}">Наши контакты</a>
                <a href="{{ path('products') }}">Все товары</a>
                <a href="{{ path('categories') }}">Все категории</a>
                <a href="{{ path('brands') }}">Все бренды</a>

                {#ВРЕМЕННО АДМИН ПАНЕЛЬ#}
                <br>
                <a href="{{ path('adminMainPage') }}" target="_blank">ADMIN PANEL</a>
            </div>
        </div>
    </div>
{% endblock %}

{% block stylesheets %}

{% endblock %}
