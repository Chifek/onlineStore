{% extends 'base.html.twig' %}

{% block body %}
    <section class="jumbotron text-center">
        <div class="container">
            <h1 class="jumbotron-heading">Создание нового бренда</h1>
            <p class="lead text-muted">
                Тут вы можете создать новый бренд.
            </p>
            <p>
                <a href="{{ path('adminMainPage') }}" class="btn btn-secondary">На главную страницу</a>
            </p>
        </div>
    </section>
    <div id="wrapper">
        <div id="container">
            <div id="next">
                <h2>Добавление бренда: </h2>
                <p>
                <form action="{{ path('saveBrand') }}" method="post">
                    <label for="title_id">Имя бренда: </label>
                    <input type="text" name="title" id="title_id"> <br>
                    <label for="description_id">Описание бренда: </label> <br>
                    <textarea id="description_id" name="description" autofocus></textarea> <br>
                    <input type="submit" value="Сохранить">
                </form>
                </p>
            </div>
        </div>
    </div>
{% endblock %}

{% block stylesheets %}

{% endblock %}
